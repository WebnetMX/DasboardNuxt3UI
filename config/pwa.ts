import type { ModuleOptions } from '@vite-pwa/nuxt'
export const pwa: ModuleOptions = {
  manifest: {
    name: 'Webnet Admin',
    short_name: 'Webnet Admin',
    description: 'Webnet Admin',
    lang: 'es',
  },
}
